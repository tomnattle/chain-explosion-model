# 4. Results: The Post-Selection Audit

The results from the `V10.4` audit pipeline (`v10_4_real_cost_curve.py`) reveal that the apparent GHZ violation is a selection-driven artifact.

## 4.1 Selection Sensitivity
We scanned the soft-detector gate strength ($gate\_k$) and observed a direct correlation between data retention ($R_{gated}$) and reported Mermin $F$.
- At **high retention** ($R \approx 100\%$), the system shows near-zero violation.
- At **low retention** ($R < 10\%$), amplitude-gated events approach the quantum prediction $F \approx 4.0$.

## 4.2 Matched-Retention Random Control
To distinguish between physical mechanisms and statistical artifacts, we compared the gated results with a random control:
- **F_gated**: Reaches high values (up to 4.0) under specific amplitude-filtering rules.
- **F_random**: Remains significantly lower (near the statistical baseline) at the same retention levels.

This proves that the "violation" is not a generic property of the field but is generated by the **specific interaction between the wave-front geometry and the detection rules**.

## 4.3 Conclusion of the Audit
The provided verification script `v10_4_real_cost_curve.py` reproduces the "Real Cost Curve" which identifies the exact "Selection Tax" required to simulate non-locality. The high $F$ values reported in earlier versions (v11, v19) are confirmed to be post-selection amplifications, supporting the hypothesis that the GHZ violation is a geometric illusion.
