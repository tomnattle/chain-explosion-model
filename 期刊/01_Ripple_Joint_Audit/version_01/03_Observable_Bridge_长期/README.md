# 03_Observable_Bridge_长期（长期攻坚）

## 目标

把 `(\mu,\rho,\eta)` 从 latent fitting variables 推进到可观测映射接口。

## 阶段里程碑

1. 定义可测映射候选：`(\mu,\rho,\eta) -> observable set`
2. 给出至少一条可证伪预测链（predictive asymmetry）
3. 设计独立测量方案（不依赖当前 toy 曲线）
4. 形成 SI 映射草案与误差预算

## 退出条件（可触发 v6 独立投稿）

- 存在可独立实验验证的预测
- 映射参数具有实验定义与测量方案
- 补齐失败案例、消融、以及不确定性分析

## 当前状态

未启动（冻结），不占用 Bell 首投带宽。

